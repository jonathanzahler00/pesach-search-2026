# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Next.js build output
.next/
out/

# Production
dist/

# Environment variables — never commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel deployment metadata
.vercel

# OS/editor artifacts
.DS_Store
Thumbs.db
*.log
*.tsbuildinfo

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Large PDFs that exceed GitHub's 100 MB file limit
# (parsed data lives in src/data/items.json — add these manually to Vercel via dashboard)
public/pdfs/COR-Passover-2026_final.pdf
